diff --git a/at_command.c b/at_command.c
index 5f0137e..2956cf4 100644
--- a/at_command.c
+++ b/at_command.c
@@ -160,7 +160,7 @@ EXPORT_DEF int at_enqueue_initialization(struct cpvt *cpvt, at_cmd_t from_comman
 		ATQ_CMD_DECLARE_ST(CMD_AT_CMGF, "AT+CMGF=0\r"),
 
 		/* SMS Storage Selection */
-		ATQ_CMD_DECLARE_ST(CMD_AT_CPMS, "AT+CPMS=\"SM\",\"SM\",\"SM\"\r"),
+		ATQ_CMD_DECLARE_ST(CMD_AT_CPMS, "AT+CPMS=\"ME\",\"ME\",\"ME\"\r"),
 		/* New SMS Notification Setting +CNMI=[<mode>[,<mt>[,<bm>[,<ds>[,<bfr>]]]]] */
 		/* pvt->initialized = 1 after successful of CMD_AT_CNMI */
 		ATQ_CMD_DECLARE_ST(CMD_AT_CNMI, "AT+CNMI=2,1,0,2,0\r"),
diff --git a/at_response.c b/at_response.c
index 1832dae..97fb3a6 100644
--- a/at_response.c
+++ b/at_response.c
@@ -200,6 +200,9 @@ static int at_response_ok (struct pvt* pvt, at_res_t res)
 			case CMD_AT_CHLD_1x:
 				CPVT_RESET_FLAGS(task->cpvt, CALL_FLAG_NEED_HANGUP);
 				ast_debug (1, "[%s] Successful hangup for call idx %d\n", PVT_ID(pvt), task->cpvt->call_idx);
+				if (task->cpvt && task->cpvt != &pvt->sys_chan && task->cpvt->channel == NULL) {
+					change_channel_state(task->cpvt, CALL_STATE_RELEASED, 0);
+				}
 				break;
 
 			case CMD_AT_CMGS:
@@ -445,6 +448,9 @@ static int at_response_error (struct pvt* pvt, at_res_t res)
 			case CMD_AT_CHUP:
 			case CMD_AT_CHLD_1x:
 				log_cmd_response_error(pvt, ecmd, "[%s] Error sending hangup for call idx %d\n", PVT_ID(pvt), task->cpvt->call_idx);
+				if (task->cpvt && task->cpvt != &pvt->sys_chan && task->cpvt->channel == NULL) {
+					change_channel_state(task->cpvt, CALL_STATE_RELEASED, 0);
+				}
 				break;
 
 			case CMD_AT_CMGR:
